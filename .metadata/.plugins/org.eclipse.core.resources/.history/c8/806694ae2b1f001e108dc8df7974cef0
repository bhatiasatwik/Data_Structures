package linkedList;

public class LinkedList {

	public static void main(String[] args) {
	NodeUse  n = new NodeUse();
	Node<Integer> head=n.input();
//	Node<Integer> head1=n.addNumber(head);
//	Node<Integer> head1=n.sort012(head);
//	head1=n.insert(head, 0, 100);
//	System.out.println(n.findNode(head,3));
//	System.out.println(n.isPalindrome(head));
//	System.out.println(n.print_I_th(head, 3));
//	int n1=n.findNode(head, 5);
//	System.out.println(n1);
	
	head = n.duplicate(head);
	n.print(head);
	}

}
  


/*if(head == null || head.next == null)
       return head;
   
   //creating three dummy nodes to point to beginning of the linked lists.
   Node <Integer>zeroD = new Node(0);
   Node<Integer> oneD = new Node(0);
   Node<Integer> twoD = new Node(0);
   
   //initializing current pointers for three lists.
   Node <Integer>zero = zeroD, one = oneD, two = twoD;
   Node<Integer> curr = head;
   
   //traversing over the list with a pointer.
   while(curr != null)
   {
       //we check data at current node and store the node in it's 
       //respective list and update the link part of that list.
       if(curr.data == 0)
       {
           zero.next = curr;
           zero = zero.next;
           curr = curr.next;
       }
       else if(curr.data == 1)
       {
           one.next = curr;
           one = one.next;
           curr = curr.next;
       }
       else
       {
           two.next = curr;
           two = two.next;
           curr = curr.next;
       }
   }
   //attaching the three lists containing 0s,1s and 2s respectively.
   zero.next = (oneD.next  != null) ? (oneD.next) : (twoD.next);
   one.next = twoD.next;
   two.next = null;
   
   //updating the head of the list.
   head = zeroD.next;
   return head;

*/



